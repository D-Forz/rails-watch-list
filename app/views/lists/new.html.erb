<div class="container">
<h1 class="text-center m-3">Create a new list</h1>
  <%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, class: 'btn btn-primary' %>
  <% end %>
</div>
